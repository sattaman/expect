{
  "name": "@browser-tester/cli",
  "version": "0.0.1",
  "description": "Browser automation with ARIA snapshots",
  "license": "MIT",
  "bin": {
    "browser-tester": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "oxlint && tsc --noEmit"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "playwright": "^1.52.0"
  },
  "devDependencies": {
    "@browser-tester/browser": "workspace:*",
    "@browser-tester/cookies": "workspace:*",
    "@types/node": "^22.15.0",
    "picocolors": "^1.1.1",
    "tsup": "^8.5.0",
    "typescript": "^5.7.0"
  }
}
